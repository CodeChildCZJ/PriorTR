# Image MLLMs

PriorTR visual-token reduction on image backbones — **LLaVA-1.5**, **InternVL2.5**,
**Qwen2-VL**, and **Qwen3-VL**. Each model is an independent subproject with its own
conda env (incompatible `transformers` pins); see the per-model subfolders and the
[root README](../README.md).
